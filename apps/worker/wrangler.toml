name = "interview-bot-api"
main = "src/index.ts"
compatibility_date = "2025-12-01"
compatibility_flags = ["nodejs_compat"]

# カスタムドメインはダッシュボードから設定
# routes = [
#   { pattern = "api.interviewbot.app", custom_domain = true }
# ]

[triggers]
crons = ["0 0 1 * *"]
